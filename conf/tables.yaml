# This part of the configuration gives information about tables
# and their relations which cannot be derived from the rdbms directly.
tables:
    sample:
        identifier:
            get by:
                module: sample
                method: getSafeNumber


